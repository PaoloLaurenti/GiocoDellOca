require 'use_case/move_player_use_case'
require 'game_events'

describe MovePlayerUseCase do
  context 'when there are only one player' do
    it 'allows the user to move the player' do
      # game_persistence_gateway = GamePersistenceGatewayTestDouble.new
      #
      # game_events = GameEvents.new
      # game_events.events << 'PLAYER_ADDED@_@Pippo'
      #
      # use_case = MovePlayerUseCase.new(game_persistence_gateway)

    end
  end
end
